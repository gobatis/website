---
sidebar_position: 4
---

# Parameter

Gobaits recognizes `#{ var }` and `${ var }` in queries as placeholders and replaces them with provided args. quotes and escapes stringly values and removes null bytes.

