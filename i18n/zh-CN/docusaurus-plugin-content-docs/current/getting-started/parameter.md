---
sidebar_position: 4
---

# 参数绑定


Gobatis 会将 SQL 中的 `#{var}` 和 `${var}` 识别为占位符，并将它们替换为所提供的参数。

## 占位符替换

使用 `#{}` 参数语法时，Gobatis 会创建 PreparedStatement 参数占位符，并通过占位符安全地设置参数（就像使用 ? 一样）。 

## 字符串替换

也可以使用 `${}` 语法直接在 SQL 语句中直接插入一个不转义的字符串。
